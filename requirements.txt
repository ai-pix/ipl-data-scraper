# Core dependencies
python-dotenv==1.0.0
google-generativeai==0.3.1
aiohttp==3.9.1
pathlib==1.0.1
typing-extensions==4.8.0
asyncio==3.4.3

# For handling JSON and datetime operations
python-dateutil==2.8.2

# Note: logging is part of Python's standard library, so we don't need to install it

# Frontend visualization libraries (referenced, not Python dependencies):
# - Chart.js v4.4.1
# - D3.js v7.8.5
# - Plotly.js v2.27.1